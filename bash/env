#Environment variables
export EDITOR=vim
export TERMINAL=rxvt-unicode
export GIT_EDITOR=vim
export DOT_REPO=https://github.com/eamoc/.config
export DOT_DEST=/home/eamoc

#Path Additions
#Set PATH so it includes user's private bin if it exists
if [ -d ~/bin ]; then
	PATH=$HOME/bin:$PATH
fi
