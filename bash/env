#Environment variables

export EDITOR="vim"
#export VULKAN_SDK="/home/eamoc/MyStuff/VulcanSDK/1.1.101.0/x86_64"
#export LD_LIBRARY_PATH="VULKAN_SDK/lib"
#export VK_LAYER_PATH="$VULKAN_SDK/etc/explicit_layer.d"
export TERMINAL="rxvt-unicode"

#Path Additions
#Set PATH so it includes user's private bin if it exists
if [ -d $HOME/bin ] ; then
	PATH="$HOME/bin:$PATH"
fi

#Set PATH so it includes AndroidSDK
#if [ -d $HOME/android-sdks/platform-tools" ] ; then
#	PATH=$PATH:$HOME/android-sdks/platform-tools
#fi

#Set PATH so it includes the Vulkan SDK
if [ -d $VULKAN_SDK/bin ] ; then
	PATH="$VULKAN_SDK/bin:$PATH"
fi





